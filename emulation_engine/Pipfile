# RETAINED — for the legacy Python emulation engine, which the app no longer
# uses (superseded by the in-process Dart engine: renode-dart + callgraph-dart).
# Kept only because the Vagrant CI harness still reads this file to provision
# Python in the test VM. See emulation_engine/engine.py. Remove with that harness.

[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
websockets = "*"
uvicorn = "*"
python-socketio = {extras = ["asyncio_client"], version = "*"}
aiofile = "*"
aionotify = "*"

[dev-packages]

[requires]
python_version = "3.12"
